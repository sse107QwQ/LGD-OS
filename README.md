# 中文 CHINESE
# 修改内容
扫雷：每次挖掘/标记会自动清屏。
暂时停更ssh（可能以后基本上不写了）
新增history命令，可通过上下键切换之前打开窗口输入的命令。
**开发者用**新增隐藏命令_log_level，无法直接输入help查看，可通过help _log_level查看该命令详细用法
# 修复的bug
扫雷：修复了挖周围无雷的地块周围的地块不自动挖掘的bug
log：由于远古老bug导致log停用，现已修复并且拓展。
# 接下来的内容
可能会加入切换语言功能，也可能加入之前调试失败的用户系统。
# 英文 ENGLISH
# Changes
Minesweeper: The screen is automatically cleared after each dig/mark.
SSH updates are temporarily suspended (probably won't be written much in the future).
Added the history command;you can use the up/down arrow keys to cycle through commands entered in previously opened windows.
**For developers**​ Added hidden command _log_level. It cannot be viewed by directly typing help; you can check its detailed usage via help _log_level.
# Fixed Bugs
Minesweeper: Fixed a bug where cells adjacent to a mine-free cell were not automatically uncovered when digging.
Log: Due to an ancient bug, the log was disabled. Now it has been fixed and expanded.
# Upcoming Content
May add language switching functionality, or may add the user system that previously failed during debugging.
# 我很闲
你猜为什么我说大话的时候要加可能？你猜呢？
另外现在Releases会加快照版本了。
